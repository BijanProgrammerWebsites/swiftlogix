name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: BijanProgrammerWebsites/hub/.github/workflows/deploy-vite.yml@master
    with:
      domain: swiftlogix.ir
      base-href: /
    secrets: inherit
